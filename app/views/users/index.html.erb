<p id="notice"></p>
<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <table class='table'>
        <tr>
          <% if @user.profile_image.attached? %>
            <%= image_tag @user.profile_image, height: '100' %>
          <% else %>
            <%= image_tag 'default-image.jpg', height: '100' %>
          <% end %>
        </tr>
        <tr>
          <th>name</th>
          <th><%= @user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th>
            <%= @user.introduction %>
          </th>
        </tr>
      </table>
  　
      <div class='row'>
        <a class="btn btn-outline-secondary btn-block edit_user_39498" href="/users/<%= @user.id %>/edit">
          <i class="fas fa-user-cog"></i>
        </a>
      </div>
      <h2 class="mt-3">New book</h2>
      <form action="/books" accept-charset="UTF-8" method="post"><input type="hidden" name="authenticity_token" value="NR2HFSEEk--TdSookCskwi4xT6iHC9Cv_u0wJ5vE5DrGv3TpUydZRTZJeQUW0McyQpTS8MilXwAHDtj0jFpVjw" autocomplete="off" />
        <div class="form-group">
          <label for="book_title">Title</label>
          <input class="form-control book_title" type="text" name="book[title]" id="book_title" />
        </div>
        <div class="form-group">
          <label for="book_opinion">Opinion</label>
          <textarea class="form-control book_body" name="book[body]" id="book_body"></textarea>
        </div>
        <div class="form-group">
          <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book" />
        </div>
      </form>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Users</h2>
      <table class='table'>
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
          <tr>
            <td>
              <% if user.profile_image.attached? %>
                <%= image_tag user.profile_image, height: '80' %>
              <% else %>
                <%= image_tag 'default-image.jpg', height: '80' %>
              <% end %>
            </td>
            <td><%= user.name %></td>
            <td><a class="user_39488" href="/users/<%= user.id %>">Show</a></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>